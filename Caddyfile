{$DOMAIN} {
    # Frontend
    handle {
        reverse_proxy web:3000
    }
    
    # API
    handle /api/* {
        reverse_proxy api:8000
    }
    
    # Uploads (static files)
    handle /uploads/* {
        reverse_proxy api:8000
    }
}
